export interface ImageData {
    id: number;
    imagePath: string;
    location: { x: number; y: number };
  }
  export interface product{
    name:string,
    image:string,
    Latitude:number,
    Longitude:number,
    City:string,
    url:File,
    id:number,
   
}